# expo-router pulls react-dom/@radix-ui (web-only) with a peer range
# that conflicts with our pinned react version. Not exercised on
# Android/iOS. See specs/pantalla-tasks/design.md.
legacy-peer-deps=true
