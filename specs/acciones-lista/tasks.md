# Acciones desde la lista — Tareas

Estado: implementado, pendiente de confirmación en vivo.

- [x] Instalar `react-native-gesture-handler`, envolver la app en
      `GestureHandlerRootView` (`app/_layout.tsx`).
- [x] `src/components/SwipeableRow.tsx`.
- [x] Integrar en las 4 listas (Tasks/Notes/Dailys/Overtime).
- [x] `npx tsc --noEmit` sin errores.
- [ ] Verificar en vivo en Android: el swipe revela Editar/Eliminar en
      cada una de las 4 listas, sin interferir con el scroll vertical
      normal de la lista.
